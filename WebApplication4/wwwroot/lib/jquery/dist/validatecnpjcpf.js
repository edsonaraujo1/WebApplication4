$(function(){
    
    $("#cpf_dep").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#CNPJ").blur(function(){
        var cnpj = $(this).val();
        if ( valida_cpf_cnpj( cnpj ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CNPJ inválido verifique !!!"
        	});
        }
    });

    $("#cnpj").blur(function(){
        var cnpj = $(this).val();
        if ( valida_cpf_cnpj( cnpj ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CNPJ inválido verifique !!!"
        	});
        }
    });

    $("#a_cnpj").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#b_cnpj").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#c_cnpj").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#d_cnpj").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#e_cnpj").blur(function () {
        var cnpj = $(this).val();
        if (valida_cpf_cnpj(cnpj)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CNPJ inválido verifique !!!"
            });
        }
    });

    $("#cnpj_edif").blur(function(){
        var cnpj = $(this).val();
        if ( valida_cpf_cnpj( cnpj ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CNPJ inválido verifique !!!"
        	});
        }
    });
    
    $("#cpf").blur(function(){
        var cpf = $(this).val();
        if ( valida_cpf_cnpj( cpf ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CPF inválido verifique !!!"
        	});
        }
    });

    $("#Cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#a_cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#b_cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#c_cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#d_cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#e_cpf").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#cpf0").blur(function(){
        var cpf = $(this).val();
        if ( valida_cpf_cnpj( cpf ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CPF inválido verifique !!!"
        	});
        }
    });

    $("#3_cpf").blur(function(){
        var cpf = $(this).val();
        if ( valida_cpf_cnpj( cpf ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CPF inválido verifique !!!"
        	});
        }
    });

    $("#1_cpf").blur(function(){
        var cpf = $(this).val();
        if ( valida_cpf_cnpj( cpf ) ) {
        } else {
        	bootbox.alert({
        		size: "small",
        		message: "CPF inválido verifique !!!"
        	});
        }
    });

    $("#e_cpf_pai").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

    $("#e_cpf_mae").blur(function () {
        var cpf = $(this).val();
        if (valida_cpf_cnpj(cpf)) {
        } else {
            bootbox.alert({
                size: "small",
                message: "CPF inválido verifique !!!"
            });
        }
    });

});
